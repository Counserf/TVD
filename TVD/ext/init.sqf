#include "\x\cba\addons\main\script_macros.hpp" // Подключение CBA для асинхронных функций
#include "..\config.sqf" // Подключение конфигурации миссии (не используется напрямую, но для единообразия)

// Инициализация расширений TVD на клиенте и сервере
if (!isDedicated) then {
    [] spawn TVD_markBox; // Запуск маркировки ящиков на клиенте (асинхронно)
};
[] spawn TVD_frisk; // Запуск механики обыска на всех машинах (асинхронно)