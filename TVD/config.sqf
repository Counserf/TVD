// TVD/config.sqf
// Основной файл конфигурации миссии TVD

// Количество зон захвата (0 — без зон, 1+ — с маркерами mZone_0, mZone_1 и т.д.)
// Задаёт, сколько точек на карте игроки должны будут захватывать для получения очков.
// Если 0, зоны не используются. Если больше 0, нужно создать маркеры в редакторе (например, mZone_0, mZone_1).
TVD_CapZonesCount = 0;

// Разрешение отступления для основных сторон (true — да, false — нет)
// Массив из двух значений: первое для bluefor (например, WEST), второе для opfor (например, EAST).
// Если true, игроки смогут отступать к базе при определённых потерях (см. TVD_RetreatRatio).
// Если false, отступление будет запрещено для этой стороны.
TVD_RetreatPossible = [true, true];

// Очки за контроль одной зоны
// Количество очков, которое получает сторона за удержание одной зоны захвата (например, mZone_0).
// Чем выше значение, тем важнее становятся зоны для победы. Обычно от 10 до 100.
TVD_ZoneGain = 50;

// Порог потерь для отступления (от 0 до 1)
// Определяет, при каком уровне потерь игроки смогут отступать к базе.
// Значение от 0 до 1: 0 — сразу можно отступать, 1 — только при полных потерях.
// Пример: 0.3 означает, что отступление доступно, когда осталось 30% или меньше от начального числа игроков.
TVD_RetreatRatio = 0.3;

// Очки за одного солдата при подсчёте начальной силы
// Сколько очков добавляется в начальный счёт за каждого обычного солдата (не ценного юнита).
// Используется для расчёта общего баланса сил в начале миссии. Обычно от 5 до 20.
TVD_SoldierCost = 10;

// Возможность продления времени миссии командиром (только для blueforSide и opforSide)
// Если true, командиры сторон (КС) смогут продлить миссию, когда время почти истекло.
// Работает только для игроков, указанных как sideLeader в TVD_Sides (blueforSide, opforSide).
// Если false, миссия завершится строго по таймеру (см. a3a_endMissionTime).
TVD_TimeExtendPossible = false;

// Максимальное количество продлений (по 5 минут каждое)
// Сколько раз командир может продлить миссию, если TVD_TimeExtendPossible = true.
// Каждое продление добавляет 5 минут. Например, 2 — это +10 минут максимум.
TVD_TimeExtendLimit = 2;

// Порог тяжёлых потерь для основных сторон (bluefor, opfor)
// Массив из двух значений (от 0 до 1), определяет критический уровень потерь для каждой стороны и их союзников.
// Если доля оставшихся игроков падает ниже этого значения, миссия завершится из-за тяжёлых потерь.
// Пример: [0.5, 0.5] — миссия закончится, если любая сторона потеряет половину или больше своих сил.
TVD_CriticalLossRatio = [0.5, 0.5];

// Время миссии в секундах (например, 3600 = 1 час)
// Устанавливает длительность миссии в секундах. По истечении времени миссия завершится автоматически.
// Пример: 3600 = 1 час, 7200 = 2 часа. Если TVD_TimeExtendPossible = true, КС может продлить это время.
missionNamespace setVariable ["a3a_endMissionTime", 3600, true];