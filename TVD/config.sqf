// Конфигурация миссии TVD

// TVD_Sides: Массив сторон, участвующих в миссии. Определяет, какие фракции будут сражаться.
// Элементы массива:
// - Элемент 0: Первая сторона (например, west - НАТО).
// - Элемент 1: Вторая сторона (например, east - РФ).
// Влияние: Указывает, кто участвует в конфликте. Порядок важен для соответствия другим массивам (например, TVD_RetreatPossible).
TVD_Sides = [west, east];

// TVD_CapZonesCount: Число зон захвата, которые будут отслеживаться в миссии.
// Значение: Целое число (0 - без зон, 1+ - количество зон с маркерами mZone_0, mZone_1 и т.д.).
// Влияние: Определяет, сколько зон нужно создать в редакторе (например, для 3 зон создайте mZone_0, mZone_1, mZone_2). Зоны дают очки (TVD_ZoneGain).
TVD_CapZonesCount = 0;

// TVD_RetreatPossible: Массив, разрешающий или запрещающий отступление для каждой стороны.
// Элементы массива:
// - Элемент 0: Разрешение для west (true - разрешено, false - запрещено).
// - Элемент 1: Разрешение для east (true - разрешено, false - запрещено).
// Влияние: Если false, игроки данной стороны не смогут отступать через базы (trgBase_sideX), даже при достижении порога TVD_RetreatRatio.
TVD_RetreatPossible = [true, true];

// TVD_ZoneGain: Количество очков, получаемых за контроль одной зоны захвата.
// Значение: Целое число (например, 50 или 100).
// Влияние: Добавляется к общему счёту стороны за каждую контролируемую зону. Влияет на итоговую победу.
TVD_ZoneGain = 50;

// TVD_RetreatRatio: Порог оставшихся игроков, при котором сторона может отступить.
// Значение: Дробное число от 0 до 1 (0.3 означает 30% остаётся, т.е. 70% потерь).
// Влияние: Когда доля живых игроков падает ниже этого значения (например, 3 из 10), игроки могут отступать через базы, если TVD_RetreatPossible = true.
TVD_RetreatRatio = 0.3;

// TVD_SoldierCost: Стоимость одного солдата в очках при подсчёте начального счёта.
// Значение: Целое число (например, 10).
// Влияние: Умножается на количество солдат в начале миссии для определения стартовой силы сторон. Влияет на общий счёт.
TVD_SoldierCost = 10;

// TVD_TimeExtendPossible: Разрешение продления времени миссии командиром.
// Значение: Логическое (true - разрешено, false - запрещено).
// Влияние: Если true, командир (sideLeader) может продлить миссию через действие в меню, увеличивая a3a_endMissionTime на 5 минут.
TVD_TimeExtendPossible = false;

// TVD_TimeExtendLimit: Максимальное количество продлений времени миссии.
// Значение: Целое число (например, 2).
// Влияние: Ограничивает, сколько раз командир может добавить 5 минут к a3a_endMissionTime. Работает только при TVD_TimeExtendPossible = true.
TVD_TimeExtendLimit = 2;

// TVD_BaseTriggers: Массив триггеров баз для каждой стороны.
// Элементы массива:
// - Элемент 0: Триггер базы west (trgBase_side0).
// - Элемент 1: Триггер базы east (trgBase_side1).
// Влияние: Указывает зоны, где игроки могут отступать или отправлять юниты в резерв. Должны быть созданы в редакторе с указанными именами.
TVD_BaseTriggers = [trgBase_side0, trgBase_side1];

// TVD_CriticalLossRatio: Массив порогов критических потерь для каждой стороны.
// Элементы массива:
// - Элемент 0: Порог для west (0.5 - 50% остаётся, т.е. 50% потерь).
// - Элемент 1: Порог для east (0.5 - 50% остаётся, т.е. 50% потерь).
// - Элемент 2: Порог для resistance (0.5 - 50% остаётся, т.е. 50% потерь).
// Влияние: Если доля оставшихся игроков падает ниже указанного значения (например, 5 из 10 для 0.5), миссия завершается с причиной "Тяжёлые потери".
TVD_CriticalLossRatio = [0.5, 0.5, 0.5];